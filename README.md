# LibOrganic

Memory that dies if you don't feed it.

Minimal C++ library providing `Organic<T>`: a biodegradable memory wrapper that decays when not accessed.

Quick build

```bash
mkdir build && cd build
cmake ..
cmake --build . --parallel
```

Run the example:

```bash
./example
```

